# file to acess the input boxes 
# number to recored the user entered batch size
# use joblib to access the saved models 
# Display the images in the card
# Store the images in a database associated ot a user ( OPTIONAL )
# First Store the images in a database then display in card ( OPTIONAL )

